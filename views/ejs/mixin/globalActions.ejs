<% if (adminpanel.menuHelper.hasGlobalActions(entity.config, actionType)) { %>
    <% var actions = adminpanel.menuHelper.getGlobalActions(entity.config, actionType) %>
    <% if (actions && actions.length > 0) { %>
        <% actions.forEach(function(action) { %>
            <% if (havePermission(action.accessRightsToken, req.session.UserAP)) { %>
                <a href="<%= action.link %>" id="<%= action.id %>">
                    <button class="btn btn-success">
                        <i class="las la-<%= action.icon %>"></i>
                        &nbsp;<%= action.title %>
                    </button>
                </a>
            <% } %>
        <% }) %>
    <% } %>
<% }%>

